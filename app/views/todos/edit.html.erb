<div class="container">
  <%= form_for [@trip, @todo] do |f| %>
    <%= f.label :title, "Fun stuff" %>
    <%= f.text_field :title %>
    <%= f.submit "Update", class: "btn btn-color" %>
  <% end %>
</div>
